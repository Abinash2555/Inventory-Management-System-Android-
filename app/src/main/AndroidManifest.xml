<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.inventory_management_system_app">

    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/inventorylogo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/inventorylogo"
        android:supportsRtl="true"
        android:theme="@style/Theme.Inventory_management_system_App">
        <activity android:name=".ScanCodeActivitydel"></activity>
        <activity android:name=".ScanCodeActivitysearch" />
        <activity android:name=".ScanCodeActivity" />
        <activity android:name=".viewInventoryActivity" />
        <activity android:name=".scanItemsActivity" />
        <activity android:name=".deleteItemsActivity" />
        <activity android:name=".additemActivity" />
        <activity android:name=".dashboardActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>